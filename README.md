# Sample - mobile first project using React + TypeScript + Vite + MaterialUI + Formik + Context API + react-chartjs-2 + styled-components

1 - run the command npm install
2 - run the command npm run start:dev
